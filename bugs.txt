Known Current bugs:
IOS - Safari:
-play both doesn't work on the athlete video side
-athlete videos show up black when uploading and changing in/out of fullscreen

Mobile (portrait POV):
-phone view is too small to fit all the videoplayer settings (fullscreen button doesn't fit)

Mobile (landscape POV):
-cannot see the videos at all. The app must detect when landscape mode activates; 
Make sure the Play Both and bottom nav bar are not stuck to the bottom to allow for scrolling and touchscreen across the screen.
Allow the app to scroll down so the user can swipe around (especially for when they zoom in)

The flow of mobile UI is unusable currently so please make these changes and necessary steps to have a clean mobile friendly profile


Ideas:
Pull up a different video instead of wc skier (2 options for upload)
Annotations button: freehand + straigh lines + give degree angle between 2 lines
Essentially screen record that takes mic input + records screen and can send it to google drives

