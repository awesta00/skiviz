Known Current bugs:
Ipad, Safari:
-play both doesn't work on the athlete video side
-athlete videos show up black when uploading and changing in/out of fullscreen
-bottom nav bar is only visible by a pixel, and can't click on it
-can't scroll down to view nav bar

Mobile (portrait POV):
-phone view is too small to fit all the videoplayer settings (fullscreen, playback speed, loop button doesn't fit). Solution, make button size smaller + enable side scrolling
-can't scroll down to access nav bar or globabl video player settings

Mobile (landscape POV):
-can't scroll down (therefore can't access nav bar or globabl video player settings)
-video player buttons may be too big for some screens (shrink down a little bit)

QOL ideas:
-for mobile, when user zooms in manually on their phone, allow them to easily scroll around the screen because it currently locks after your fingers let go after zooming in (this feature already works on Ipad Safari)


Ideas:
Pull up a different video instead of wc skier (2 options for upload)
Annotations button: freehand + straigh lines + give degree angle between 2 lines
Essentially screen record that takes mic input + records screen and can send it to google drives

